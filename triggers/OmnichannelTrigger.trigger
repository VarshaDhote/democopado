trigger OmnichannelTrigger on AgentWork (before insert, before update, after insert, after update) {
         
    //if(trigger.isAfter  &&  trigger.isInsert ){
        
    //   OmniChannelClass.agentWork(trigger.new);
    //for(AgentWork ag : trigger.new){
      // System.debug('Check trigger');
    //}
}